# Copy this file to .env and fill in your keys. .env is gitignored.
# Both providers offer a free tier sufficient for a single live event.

# Groq API key — fast LLM extraction (Llama 3.3 70B).
GROQ_API_KEY=

# Google Gemini API key (AI Studio) — tone analysis (Gemini 2.5 Flash).
GEMINI_API_KEY=
